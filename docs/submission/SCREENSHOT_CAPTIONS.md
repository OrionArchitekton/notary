# Screenshot captions (Devpost upload order; each <= 140 chars)

1. `01-replay-hero.png`
   The hosted replay: a real captured Notary run, disclosed on-page, with the honest evaluation table published misses included.

2. `02-run-cli.png`
   One command: Notary probes the warehouse, contradicts the USD claim (median 12795, integer cents), and writes verdicts back.

3. `03-datahub-schema.png`
   DataHub after the run: the amount column carries a provenance-labeled correction pointing at the evidence dossier.

4. `04-datahub-incident.png`
   The danger gate: a contradicted unit claim on a high-traffic asset raises a real operational incident in DataHub.

5. `05-s5-flip.png`
   Same catalog, two answers: without the trust ledger the agent can only relay the unverified claim; with it, it refuses and cites evidence.

6. `06-honest-table.png`
   The scorecard we publish verbatim: 12 planted lies, 9 caught, 0 false positives, misses disclosed with reasons in the README.
